import { helper } from '@ember/component/helper';

export function deliveryCharge(params/*, hash*/) {
  let deliveryCharge = 50;
  
  //return params[0].length * deliveryCharge;
  //return params;
}

export default helper(deliveryCharge);
