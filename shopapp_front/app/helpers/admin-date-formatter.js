import { helper } from '@ember/component/helper';

export function adminDateFormatter(params/*, hash*/) {
  //const string = params[0].split(" ");
  return params

}

export default helper(adminDateFormatter);
